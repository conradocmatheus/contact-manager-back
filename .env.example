DATABASE_URL="postgresql://postgres:postgres@localhost:5432/contacts_manager?schema=public"
JWT_SECRET="replace-with-a-secure-secret"
BCRYPT_SALT_ROUNDS=10
CORS_ORIGIN="http://localhost:4200"

# NumberVerify Configuration
NUMVERIFY_API_KEY="your_api_key"
NUMVERIFY_API_URL="https://api.apilayer.com/number_verification/validate"

# AWS RDS example (use a secret store; never commit the real value):
# DATABASE_URL="postgresql://user:password@rds-endpoint:5432/contacts_manager?schema=public&sslmode=require"
